server:
  port: 8083

spring:
  application:
    name: shipment-service
  datasource:
    url: jdbc:postgresql://localhost:5432/logiflow_db
    username: logiflow
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
