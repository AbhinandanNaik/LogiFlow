server:
  port: 8082

spring:
  application:
    name: tracking-service
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: password
